# 🎨 VISUAL DIAGRAMS & FLOWCHARTS
## Clinic Queue Management System - Visual Guide

---

## 📊 SYSTEM OVERVIEW DIAGRAM

```
┌───────────────────────────────────────────────────────────────┐
│                    CLINIC QUEUE SYSTEM                        │
│                   (Priority Queue Based)                      │
└───────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌───────────────────────────────────────────────────────────────┐
│                      USER INTERFACE                           │
│  ┌──────────────────────────────────────────────────────┐    │
│  │  Main Menu:                                           │    │
│  │  [1] Add Patient      [6] Display Queue              │    │
│  │  [2] Call Next        [7] Peek Next                  │    │
│  │  [3] Remove Patient   [8] Statistics                 │    │
│  │  [4] Update Patient   [9] About                      │    │
│  │  [5] Search Patient   [0] Exit                       │    │
│  └──────────────────────────────────────────────────────┘    │
└───────────────────────┬───────────────────────────────────────┘
                        │
                        ▼
┌───────────────────────────────────────────────────────────────┐
│                   CLINIC QUEUE CLASS                          │
│  ┌─────────────────────────────────────────────────────┐     │
│  │  Data Structures:                                    │     │
│  │  • PriorityQueue<Patient> - Main queue (Min-Heap)   │     │
│  │  • HashMap<ID, Patient> - Fast ID lookup (O(1))     │     │
│  └─────────────────────────────────────────────────────┘     │
│  ┌─────────────────────────────────────────────────────┐     │
│  │  Operations:                                         │     │
│  │  • addPatient() → INSERT                             │     │
│  │  • callNextPatient() → DELETE                        │     │
│  │  • updatePatient() → UPDATE                          │     │
│  │  • searchByID/Name/Priority() → SEARCH              │     │
│  └─────────────────────────────────────────────────────┘     │
└───────────────────────┬───────────────────────────────────────┘
                        │
                        ▼
┌───────────────────────────────────────────────────────────────┐
│                     PATIENT CLASS                             │
│  ┌─────────────────────────────────────────────────────┐     │
│  │  Attributes:                                         │     │
│  │  • patientId (String)                                │     │
│  │  • name (String)                                     │     │
│  │  • age (int)                                         │     │
│  │  • condition (String)                                │     │
│  │  • priority (int): 1=Critical, 2=Urgent, 3=Regular  │     │
│  │  • arrivalTime (long) - for FIFO within priority    │     │
│  │  • status (String): Waiting/In Treatment/Completed  │     │
│  └─────────────────────────────────────────────────────┘     │
│  ┌─────────────────────────────────────────────────────┐     │
│  │  Methods:                                            │     │
│  │  • compareTo(Patient) - implements Comparable        │     │
│  │  • Getters & Setters                                 │     │
│  │  • toString() - formatted display                    │     │
│  └─────────────────────────────────────────────────────┘     │
└───────────────────────────────────────────────────────────────┘
```

---

## 🌳 PRIORITY QUEUE (MIN-HEAP) STRUCTURE

```
Priority Queue Internal Structure (Min-Heap):
==============================================

Example with 5 patients:
                    P1 (Priority 1)
                   /               \
           P3 (Priority 2)      P4 (Priority 3)
           /        \
    P5 (Priority 2)  P6 (Priority 3)


As Array: [P1(1), P3(2), P4(3), P5(2), P6(3)]
          Index: [0,     1,     2,     3,     4]

Parent of index i: (i-1)/2
Left child of i:   2i+1
Right child of i:  2i+2

Heap Property: Parent Priority ≤ Children Priority
```

---

## 🔄 OPERATION FLOWCHARTS

### 1. INSERT Operation (Add Patient)

```
START
  │
  ▼
┌─────────────────────┐
│ User enters patient │
│ information         │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐      YES   ┌──────────────┐
│ Patient ID exists?  ├───────────►│ Show error   │──► END
└──────────┬──────────┘             └──────────────┘
           │ NO
           ▼
┌─────────────────────┐
│ Create Patient obj  │
│ with timestamp      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Add to PriorityQueue│  ← O(log n) heap insertion
│ (Heap insert)       │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Add to HashMap      │  ← O(1) insertion
│ (ID → Patient)      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Increment stats     │
│ Show success message│
└──────────┬──────────┘
           │
           ▼
          END
```

### 2. DELETE Operation (Call Next Patient)

```
START
  │
  ▼
┌─────────────────────┐      YES   ┌──────────────┐
│ Queue empty?        ├───────────►│ Show error   │──► END
└──────────┬──────────┘             └──────────────┘
           │ NO
           ▼
┌─────────────────────┐
│ Poll from Queue     │  ← O(log n) heap deletion
│ (Remove root/min)   │     (removes highest priority)
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Remove from HashMap │  ← O(1) deletion
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Update patient      │
│ status to           │
│ "In Treatment"      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Increment served    │
│ counter             │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Display patient     │
│ information         │
└──────────┬──────────┘
           │
           ▼
          END
```

### 3. UPDATE Operation

```
START
  │
  ▼
┌─────────────────────┐
│ Enter Patient ID    │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐      NO    ┌──────────────┐
│ Search in HashMap   ├───────────►│ Show error   │──► END
│ Patient found?      │             └──────────────┘
└──────────┬──────────┘
           │ YES
           ▼
┌─────────────────────┐
│ Show current info   │
│ Get new values      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Remove patient from │  ← O(n) - linear search in queue
│ PriorityQueue       │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Update patient      │
│ fields              │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Re-add to Queue     │  ← O(log n) - heap insertion
│ (new position based │     (automatically reordered)
│  on new priority)   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Update HashMap      │  ← O(1)
│ Show success        │
└──────────┬──────────┘
           │
           ▼
          END
```

### 4. SEARCH Operation

```
START
  │
  ▼
┌─────────────────────┐
│ Choose search type: │
│ [1] By ID           │
│ [2] By Name         │
│ [3] By Priority     │
└──────────┬──────────┘
           │
     ┌─────┴─────┬──────────┐
     │           │          │
     ▼           ▼          ▼
┌─────────┐ ┌─────────┐ ┌──────────┐
│ By ID   │ │ By Name │ │ Priority │
│ O(1)    │ │ O(n)    │ │ O(n)     │
└────┬────┘ └────┬────┘ └────┬─────┘
     │           │          │
     │           │          │
     ▼           ▼          ▼
┌──────────────────────────────┐
│ Search in HashMap / Iterate  │
│ through all patients         │
└──────────┬───────────────────┘
           │
    ┌──────┴──────┐
    │             │
   YES            NO
    │             │
    ▼             ▼
┌─────────┐  ┌─────────┐
│ Display │  │ Show    │
│ results │  │ "Not    │
│         │  │ Found"  │
└────┬────┘  └────┬────┘
     │            │
     └────────┬───┘
              │
              ▼
             END
```

---

## 🎯 PRIORITY ORDERING VISUAL

```
Queue Order by Priority (Automatic):
=====================================

Patients arrive:
┌──────────────────────────────────┐
│ Time 10:00 → P1 (Regular)    [3] │
│ Time 10:05 → P2 (Critical)   [1] │ 
│ Time 10:10 → P3 (Urgent)     [2] │
│ Time 10:15 → P4 (Regular)    [3] │
│ Time 10:20 → P5 (Critical)   [1] │
└──────────────────────────────────┘

After Priority Queue Ordering:
┌──────────────────────────────────┐
│ Position 1: P2 [1] - Critical    │ ← Called FIRST
│ Position 2: P5 [1] - Critical    │
│ Position 3: P3 [2] - Urgent      │
│ Position 4: P1 [3] - Regular     │
│ Position 5: P4 [3] - Regular     │ ← Called LAST
└──────────────────────────────────┘

Within same priority (e.g., P2 and P5):
→ FIFO: P2 arrived first (10:05) → P2 before P5
```

---

## 📈 TIME COMPLEXITY VISUAL

```
Operation Comparison:
=====================

INSERT (Add Patient):
┌─────────────────────────────────────┐
│ Array:         O(n)    ████████████ │
│ PriorityQueue: O(log n)  ████       │ ← Winner!
└─────────────────────────────────────┘

DELETE (Call Next):
┌─────────────────────────────────────┐
│ Array:         O(n)    ████████████ │
│ PriorityQueue: O(log n)  ████       │ ← Winner!
└─────────────────────────────────────┘

SEARCH by ID:
┌─────────────────────────────────────┐
│ No HashMap:    O(n)    ████████████ │
│ With HashMap:  O(1)    █            │ ← Winner!
└─────────────────────────────────────┘

Scalability (1000 patients):
┌─────────────────────────────────────┐
│ O(1):      1 operation               │ ✓ Excellent
│ O(log n):  ~10 operations            │ ✓ Good
│ O(n):      1000 operations           │ ⚠ Acceptable
│ O(n²):     1,000,000 operations      │ ✗ Poor
└─────────────────────────────────────┘
```

---

## 🔄 PATIENT STATUS LIFECYCLE

```
Patient Journey Through System:
================================

   ┌─────────────┐
   │  Patient    │
   │  Arrives    │
   └──────┬──────┘
          │
          ▼
   ┌─────────────┐
   │  Register   │ ← addPatient()
   │  at Clinic  │    (INSERT)
   └──────┬──────┘
          │
          ▼
   ┌─────────────┐
   │   WAITING   │ ← Status: "Waiting"
   │   IN QUEUE  │    In PriorityQueue
   └──────┬──────┘
          │
          │ (Automatic ordering by priority)
          │
          ▼
   ┌─────────────┐
   │ Called for  │ ← callNextPatient()
   │ Treatment   │    (DELETE from queue)
   └──────┬──────┘
          │
          ▼
   ┌─────────────┐
   │    IN       │ ← Status: "In Treatment"
   │ TREATMENT   │    No longer in queue
   └──────┬──────┘
          │
          ▼
   ┌─────────────┐
   │  Treatment  │
   │  Complete   │ ← Status: "Completed"
   └──────┬──────┘
          │
          ▼
   ┌─────────────┐
   │   Patient   │
   │   Leaves    │
   └─────────────┘
```

---

## 🎨 USER INTERFACE FLOW

```
Main Menu Flow:
===============

         START
           │
           ▼
    ┌──────────────┐
    │ Display Menu │◄─────────┐
    └──────┬───────┘          │
           │                  │
           ▼                  │
    ┌──────────────┐          │
    │ Get User     │          │
    │ Choice       │          │
    └──────┬───────┘          │
           │                  │
    ┌──────┴────────────────┐ │
    │                       │ │
    ▼                       ▼ │
┌────────┐            ┌────────┐
│Option 1│            │Option N│
│Add Pat.│   ...      │  Exit  │
└───┬────┘            └───┬────┘
    │                     │
    ▼                     ▼
┌────────┐            ┌────────┐
│Execute │            │ Confirm│
│Operation│           │  Exit  │
└───┬────┘            └───┬────┘
    │                     │
    ▼                     ▼
┌────────┐            ┌────────┐
│Display │            │  END   │
│Result  │            └────────┘
└───┬────┘
    │
    ▼
┌────────┐
│  Wait  │
│  for   │
│ Enter  │
└───┬────┘
    │
    └────────────────────┘
```

---

## 🏥 CLINIC SCENARIO EXAMPLE

```
Real-World Scenario:
====================

Time: Monday Morning at Clinic

10:00 AM:
┌─────────────────────────────────┐
│ P001: John, 65, Regular Checkup │ Priority: 3
└─────────────────────────────────┘
   Queue: [P001(3)]

10:15 AM:
┌─────────────────────────────────┐
│ P002: Sarah, 28, Severe Headache│ Priority: 2
└─────────────────────────────────┘
   Queue: [P002(2), P001(3)]
          └─ Urgent patient jumps ahead!

10:30 AM:
┌─────────────────────────────────┐
│ P003: Mike, 45, Chest Pain      │ Priority: 1
└─────────────────────────────────┘
   Queue: [P003(1), P002(2), P001(3)]
          └─ Emergency! Goes to front!

10:35 AM:
┌─────────────────────────────────┐
│ P004: Emma, 35, Regular Checkup │ Priority: 3
└─────────────────────────────────┘
   Queue: [P003(1), P002(2), P001(3), P004(3)]
                             └────────┴────────┘
                               FIFO: P001 before P004

Doctor calls next patient:
   → P003 (Mike) called first - Critical case!
   Queue: [P002(2), P001(3), P004(3)]

   → P002 (Sarah) called next - Urgent case
   Queue: [P001(3), P004(3)]

   → P001 (John) called - Regular, arrived first
   Queue: [P004(3)]

   → P004 (Emma) called last
   Queue: []
```

---

## 📊 STATISTICS VISUALIZATION

```
System Statistics Display:
==========================

┌─────────────────────────────────────┐
│  CLINIC QUEUE STATISTICS            │
├─────────────────────────────────────┤
│ Total Admitted:  ████████  25       │
│ Total Served:    ██████    18       │
│ Currently Waiting: ██      7        │
├─────────────────────────────────────┤
│ BY PRIORITY:                        │
│ Critical (1): █    2                │
│ Urgent (2):   ██   3                │
│ Regular (3):  ██   2                │
├─────────────────────────────────────┤
│ Average Wait: ~15 minutes           │
│ Status: Normal Operations           │
└─────────────────────────────────────┘
```

---

## 🔍 DUAL DATA STRUCTURE ADVANTAGE

```
Why PriorityQueue + HashMap?
=============================

Scenario: Find and update patient P042

Option 1: Only PriorityQueue
┌─────────────────────────────────────┐
│ 1. Search for P042 in queue: O(n)  │
│    → Must check each patient        │
│ 2. Remove from queue: O(log n)     │
│ 3. Update and re-add: O(log n)     │
│ Total: O(n) + O(log n) ≈ O(n)      │
└─────────────────────────────────────┘

Option 2: PriorityQueue + HashMap ✓
┌─────────────────────────────────────┐
│ 1. Find in HashMap: O(1)            │
│    → Instant lookup by ID!          │
│ 2. Remove from queue: O(log n)     │
│ 3. Update and re-add: O(log n)     │
│ Total: O(1) + O(log n) ≈ O(log n)  │
└─────────────────────────────────────┘

Result: Much faster for ID-based operations! ✓
```

---

## 📐 HEAP OPERATIONS VISUALIZED

### Heapify-Up (Insert P999 with Priority 1)

```
Step 1: Add to end
        P2(1)
       /     \
    P5(2)    P7(3)
    /  \      /
 P8(2) P9(3) P999(1) ← New

Step 2: Compare with parent P7(3)
        P2(1)
       /     \
    P5(2)    P7(3) ← P999(1) < P7(3)
    /  \      /
 P8(2) P9(3) P999(1) ✓ SWAP!

Step 3: After swap
        P2(1)
       /     \
    P5(2)    P999(1) ← Compare with P2(1)
    /  \      /
 P8(2) P9(3) P7(3)

P999(1) = P2(1), no more swaps needed
Heap property maintained! ✓
```

### Heapify-Down (Remove root)

```
Step 1: Remove root P2(1), move last to root
        P7(3) ← Last element
       /     \
    P5(2)    P999(1)
    /  \    
 P8(2) P9(3)

Step 2: Compare with children
        P7(3) ← Compare with P5(2) and P999(1)
       /     \
    P5(2)    P999(1) ✓ P999 is smaller
    /  \    
 P8(2) P9(3)
SWAP with smallest child!

Step 3: After swap
        P999(1) ✓
       /       \
    P5(2)      P7(3)
    /  \    
 P8(2) P9(3)

Heap property restored! ✓
```

---

## 🎯 PRESENTATION VISUAL AIDS

### Show This During Demo:

```
"Our Priority Queue Automatically Orders Patients!"

Before (Order of Arrival):
→ → → → → → → →
[P1(3)] [P2(1)] [P3(2)] [P4(3)]

After (Automatic Priority Ordering):
↓
[P2(1)] [P3(2)] [P1(3)] [P4(3)]
  ↑       ↑       ↑       ↑
Critical Urgent Regular Regular
```

---

## ✅ DEMO SEQUENCE VISUAL

```
5-Minute Demo Path:
===================

[Start] → [Show Menu] → [Display Initial Queue]
            ↓
        [Add Emergency Patient]
            ↓
        [Show Updated Queue] ← Patient jumped to front!
            ↓
        [Call Next Patient] ← Emergency patient treated
            ↓
        [Search Patient] ← Fast O(1) ID search
            ↓
        [Update Patient Priority] ← Automatic reordering
            ↓
        [Show Statistics] ← Professional reporting
            ↓
        [Explain Data Structure] ← Highlight Priority Queue
            ↓
        [Ready for Questions] ← Confident!
```

---

**Use these diagrams during our presentation to visually explain concepts! 📊**

**Print this file or have it open for reference during demo! 🖨️**
